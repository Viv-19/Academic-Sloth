# 🎓 LEARNING: Every directory in Python that should be treated as a
# "package" (importable module) needs an __init__.py file.
# It can be empty — its presence is what matters.
# This is Python's equivalent of index.js in Node.js modules.
