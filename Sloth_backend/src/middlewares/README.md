# Middlewares
Functions that run *before* the request reaches the Controller.
Examples: 
- `authMiddleware`: Checking if the user is logged in.
- `uploadMiddleware`: Handling file uploads (like PDFs) before hitting the route.
